// 논리 연산자: 여러 조건의 조합 (and && or || 연산자)

let isAdult = true;
let hasTicket = false;

// and 연산 => && 사용
let canPass = isAdult && hasTicket
console.log(canPass);

// or 연산 => || 사용
let canPass2 = isAdult || hasTicket
console.log(canPass2);

